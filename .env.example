# Server-side mail config
# If using the provided `api/sendEmail.js`, set the SMTP variables below.
# For Vercel set these in the UI, locally add to `.env`.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user@example.com
SMTP_PASS=your_smtp_password
CONTACT_EMAIL=your_recipient_email@example.com
CONTACT_DISPLAY_NAME=Your Name
